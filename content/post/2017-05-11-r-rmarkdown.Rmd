---
title: "Hello R Markdown"
author: "Timothy Lin"
draft: false
date: 2017-05-11T21:13:14-05:00
categories: ["R"]
tags: ["R Markdown", "plot", "regression"]
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(collapse = TRUE)
```

# R Markdown

[![vancouver](http://weknownyourdreamz.com/images/seaside/seaside-04.jpg)](http://weknownyourdreamz.com/seaside.html)

This is a R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

You can embed an R code chunk like this:

```{r cars}
summary(cars)
fit <- lm(dist ~ speed, data = cars)
fit

```

# Including Plots

You can also embed plots. See Figure \@ref(fig:pie) for example:

```{r pie, fig.cap='A fancy pie chart.', tidy=FALSE}
par(mar = c(0, 1, 0, 1))
pie(
  c(280, 60, 20),
  c('Sky', 'Sunny side of pyramid', 'Shady side of pyramid'),
  col = c('#0292D8', '#F7EA39', '#C4B632'),
  init.angle = -50, border = NA
)
```

Test of scatterplot with ggplot and plotly 

```{r scatter, fig.cap='Plotly', message=FALSE}
library(ggplot2)
library(plotly)
x<-seq(1,10,1)
y<-x + rnorm(n=10, mean=0, sd=3)
scatter<-ggplot(as.data.frame(cbind(x,y)), aes(x,y)) + geom_point() + geom_smooth()
ggplotly(scatter)
```

Using shortcodes with rmd

```{r shortcode}
blogdown::shortcode('tweet', '666616452582129664')
```

```{r shortcode2}
blogdown::shortcode('youtube', 'w7Ft2ymGmfc')
```
